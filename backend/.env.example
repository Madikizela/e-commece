# Database Configuration
DB_CONNECTION_STRING=Host=localhost;Database=ecommerce;Username=postgres;Password=your_password

# Email Configuration (Required for email notifications)
# IMPORTANT: Replace these with your actual email credentials
# The application will work without email, but users won't receive notifications

# Gmail Setup (Recommended):
# 1. Enable 2-factor authentication on your Gmail account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the generated App Password (16 characters) as EMAIL_PASSWORD
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=madikizela21517799@gmail.com
EMAIL_PASSWORD=nsfsozpfgkjwgwdt
EMAIL_FROM=madikizela21517799@gmail.com

# Alternative SMTP providers:
# Outlook: smtp-mail.outlook.com (port 587)
# Yahoo: smtp.mail.yahoo.com (port 587)
# Custom SMTP: your_smtp_server.com (check with provider for port)

# JWT Configuration
JWT_KEY=your_super_secret_jwt_key_here_make_it_long_and_secure
JWT_ISSUER=EcommerceAPI
JWT_AUDIENCE=EcommerceClient

# Admin Configuration
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=your_secure_admin_password